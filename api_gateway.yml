server:
  port: 8085

spring:
  application:
    name: api_gateway

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

      routes:
        - id: user_service
          uri: lb://user_service
          predicates:
            - Path=/api/users/**

        - id: restaurant_service
          uri: lb://restaurant_service
          predicates:
            - Path=/api/restaurants/**

        - id: meal_service
          uri: lb://meal_service
          predicates:
            - Path=/api/meals/**

        - id: order_service
          uri: lb://order_service
          predicates:
            - Path=/api/orders/**

eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info
